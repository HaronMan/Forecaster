{
  "name": "forecaster",
  "version": "1.0.0",
  "description": "",
  "main": "electron/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd forecaster-app && npm run build && cd .. && electron .",
    "dev": "cd forecaster-app && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^34.0.0"
  },
  "dependencies": {
    "forecaster": "file:"
  }
}
